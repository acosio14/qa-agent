# What is this module for?
# To summarize files? And do I summarize each one or create one large summary?
# Do I want to directly put file in LLM (they could be large and increase context)
# If direct to context then how would this file pass them? Or would it even be needed?
# And if I put directly, order matters, which go first?
# Should I create bits? Would that work? Would that actually be less context or more?
# Do I parse them to try to select the most important parts? How would I select them?
#     