[project]
name = "qa-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "openrouter>=0.11.22",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ty>=0.0.55",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
